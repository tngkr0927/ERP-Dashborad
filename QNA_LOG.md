# ERP Analytics Dashboard — 질문 & 답변 기록

> 개발 과정에서 나온 질문, 답변, 그리고 왜 그렇게 답변했는지 이유를 기록.

---

## Q1. 프로젝트 초기 골격을 만들어줘

**질문 요약:** ERP 데이터 분석 대시보드의 확장 가능한 기본 구조를 만들어 달라.

**답변:** FastAPI + PostgreSQL + React + Docker Compose 기반으로 풀스택 프로젝트를 구성함. DB에 JSONB 컬럼을 사용하고, 분석 엔진은 전략 패턴으로 설계함.

**이유:**
- **FastAPI** — ERP 데이터는 I/O 바운드 작업(DB 읽기/쓰기)이 많아서 async 지원이 중요. Django는 async 지원이 아직 미성숙하고, Flask는 async가 기본이 아님. FastAPI는 async 네이티브 + Swagger 자동 생성까지 해줘서 API 문서화 비용이 0.
- **PostgreSQL + JSONB** — ERP 데이터는 시스템마다 스키마가 다름. 정규화된 테이블로 미리 설계하면 새 데이터 소스 추가할 때마다 마이그레이션이 필요. JSONB로 원본을 유연하게 받고, 분석 결과도 JSONB로 저장하면 스키마 변경 없이 확장 가능.
- **전략 패턴** — 분석 로직은 계속 추가될 것. if-else로 분기하면 코드가 금방 스파게티가 됨. 추상 클래스 + 레지스트리 패턴을 쓰면 새 분석기를 만들 때 기존 코드를 건드리지 않고 클래스만 추가하면 됨 (OCP 원칙).
- **Docker Compose** — "내 컴퓨터에서는 되는데" 문제를 원천 차단. DB + 백엔드 + 프론트를 한 번에 띄울 수 있어서 온보딩 비용이 거의 없음.

---

## Q2. 너무 장황하다, 간결하게 줄여줘 (DEV_LOG.md)

**질문 요약:** DEV_LOG.md가 설명이 너무 길다. 읽기 쉽게 줄여달라.

**답변:** "왜 이 기술을 선택했는지" 장문 설명을 표 형태로 압축하고, 구현 내용도 핵심만 남김.

**이유:**
- 개발 기록은 나중에 빠르게 훑어보는 용도. 상세한 이유는 이 QNA_LOG.md에 남기고, DEV_LOG.md는 "뭘 만들었는지"를 한눈에 파악할 수 있는 구조가 더 적합하다고 판단.
- 기술 선택 이유를 표(`| 선택 | 이유 |`)로 바꾸면 비교도 쉽고 시각적으로 깔끔함.

---

## Q3. SETUP.md 작성해줘

**질문 요약:** 로컬 환경 세팅 가이드를 만들어줘.

**답변:** Docker Compose와 로컬 직접 실행, 두 가지 방법을 모두 안내하는 문서를 작성함.

**이유:**
- Docker를 쓸 줄 아는 사람은 `docker compose up` 한 줄이면 끝나지만, 디버깅이나 핫리로드가 필요한 개발자는 로컬에서 직접 실행하는 게 편함. 두 가지 시나리오를 모두 커버해야 실제로 쓸모 있는 문서가 됨.
- 동작 확인 체크리스트(`curl` 명령어)를 넣은 이유는, 세팅 후 "되는 건지 안 되는 건지" 확인할 방법이 없으면 삽질이 길어지기 때문.

---

## Q4. Docker 처음 쓰는 사람 기준으로 다시 써줘

**질문 요약:** SETUP.md를 Docker 경험 없는 사람도 따라할 수 있게 다시 작성해달라.

**답변:** Docker Desktop 설치부터 시작하는 Step 1~4 구조로 전면 재작성. "로컬 직접 실행" 방법은 삭제하고 Docker 방식만 남김.

**이유:**
- Docker 초보에게 두 가지 방법(Docker vs 로컬)을 동시에 보여주면 오히려 혼란. "어떤 걸 해야 하지?" 고민하게 됨. 가장 간단한 한 가지 방법만 보여주는 게 첫 경험에는 더 나음.
- Docker Desktop 설치를 Step 1으로 넣은 이유는, "docker command not found" 에러를 맨 처음에 만나면 바로 포기하기 때문. 설치 확인(`docker --version`)까지 안내해서 여기서 막히지 않게 함.
- 트러블슈팅을 에러 메시지 기반("port is already allocated", "Cannot connect to the Docker daemon")으로 쓴 이유는, 초보는 에러 메시지를 보고 해결법을 찾기 때문. 상황 설명보다 에러 메시지 그 자체를 제목으로 쓰는 게 더 직관적.
- 마지막에 "Docker가 하는 일" 섹션을 넣은 이유는, 블랙박스로 쓰면 문제가 생겼을 때 디버깅이 안 됨. 내부적으로 뭘 하는지 대략이라도 알아야 응용이 가능.

---

## Q5. DEV_LOG.md와 QNA_LOG.md 분리해줘

**질문 요약:** DEV_LOG.md에는 순수 개발 내용만, 질문/답변/이유는 별도 파일로 분리해달라.

**답변:** DEV_LOG.md는 구현 사실만 남기고, QNA_LOG.md(이 파일)를 새로 만들어 질문-답변-이유 구조로 기록.

**이유:**
- 목적이 다른 정보는 파일을 분리하는 게 맞음. DEV_LOG는 "뭘 만들었나" (사실 기반), QNA_LOG는 "왜 그렇게 했나" (의사결정 기반). 한 파일에 섞으면 나중에 원하는 정보를 찾기 어려움.
- QNA_LOG를 질문→답변→이유 3단 구조로 잡은 이유는, 나중에 비슷한 상황에서 "그때 왜 그렇게 했더라?" 를 빠르게 찾을 수 있게 하기 위함.

---

*질문이 추가될 때마다 새 항목 추가*
